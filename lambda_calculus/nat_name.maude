fmod NAT-NAME is protecting NAT .
	sort Name .
	op x{_} : Nat -> Name [ctor] .
	op _.=._ : Name Name -> Bool .

	op i : Name -> Nat .
	var n m : Nat .

	eq x{n} .=. x{m} = (n == m) .
	eq i(x{n}) = n .
endfm
